/** Re-export from @astro/shared — single source of truth for desktop + mobile. */
export {
  CLIENT_NAMES,
  CLIENT_APP_CODES,
  appCodeForName,
  type ClientName,
} from '@astro/shared/clientNames';
