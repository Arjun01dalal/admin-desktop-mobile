/**
 * @deprecated Prefer `<TokenValidator />` (laxminarayan parity).
 * Kept as a thin re-export so older imports keep working.
 */
export { useTokenValidator as useSessionGuard } from './useTokenValidator';
