/**
 * Preload for the embedded https://admin.astrothirdeye.com/ BrowserView.
 * Panel login is opened only via the app Login button (gated by SOS), not site clicks.
 */
// Intentionally empty — browsing stays inside the admin site BrowserView.
